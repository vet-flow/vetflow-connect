name: Build & Release VetFlowConnect

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        # VETFL-671: numpy PINNED do 1.26.4. numpy 2.x przeniósł rdzeń do numpy._core z
        # binarką _multiarray_umath, której PyInstaller onefile nie bundluje niezawodnie
        # (crash "No module named numpy._core._multiarray_umath" / brak _MEI...\numpy\_core
        # na starcie). 1.26.4 = stara struktura numpy.core, żelazna z PyInstallerem,
        # zgodna z pydicom/pynetdicom. Reszta świadomie niepinowana — pinuj gdy znów driftnie.
        run: pip install pyinstaller aiohttp certifi pystray Pillow pynetdicom pydicom "numpy==1.26.4" cryptography

      - name: Build EXE
        shell: bash
        run: |
          cd src
          pyinstaller --onefile --name VetFlowConnect --icon ../icon.ico --add-data "../config.json.example;." --add-data "plugins;plugins" --paths . --noconsole --hidden-import pystray --hidden-import pystray._win32 --hidden-import PIL --hidden-import PIL.Image --collect-all numpy --collect-all pynetdicom --collect-all pydicom --collect-data certifi --collect-all cryptography build_entry.py

      - name: Generate SHA256
        shell: bash
        run: |
          cd src/dist
          sha256sum VetFlowConnect.exe > VetFlowConnect.exe.sha256
          cat VetFlowConnect.exe.sha256

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: VetFlowConnect-Windows
          path: |
            src/dist/VetFlowConnect.exe
            src/dist/VetFlowConnect.exe.sha256

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            src/dist/VetFlowConnect.exe
            src/dist/VetFlowConnect.exe.sha256
            config.json.example
          generate_release_notes: true
